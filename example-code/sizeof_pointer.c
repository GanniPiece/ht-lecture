#include <stdio.h>

int main () {
	printf("size of char * = %lu\n", sizeof(char*));
	printf("size of short * = %lu\n", sizeof(short*));
	printf("size of int * = %lu\n", sizeof(int*));
	printf("size of long * = %lu\n", sizeof(long*));

	return 0;
}
